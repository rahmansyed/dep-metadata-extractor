# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# dep-metadata-extractor binary
dep-metadata-extractor

# vendor directory
vendor

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
